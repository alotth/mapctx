# T-001

  - role: backend
  - impact: high
  - estimatedEffort: 3d
  - prerequisites: []
  - blocking: [T-002, T-004]
  - filesAffected: [src/markdownParser.ts, src/roadmapWebviewPanel.ts]
  - testsRequired: [npm run test:parser, npm run check-types]
  - description: Parse `## Tasks` as a single source and map board columns from `status`.
  - acceptance:
      - [ ] Parser reads both legacy and v2 formats.
      - [ ] Serializer keeps stable task field order.
      - [ ] Roadmap progress treats `status: done` as complete.
  - steps:
      - [x] Add task-level status field support.
      - [ ] Add format detection and fallback logic.
      - [ ] Add regression tests for both formats.
    ```md
    This task establishes compatibility with the new model while preserving old boards.
    It is the main prerequisite for migration and UI tasks.
    ```
