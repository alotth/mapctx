# T-002

  - role: backend
  - impact: high
  - estimatedEffort: 2d
  - prerequisites: [T-001]
  - blocking: [T-005]
  - filesAffected: [scripts/migrate-tasks-v2.ts, README.md]
  - testsRequired: [npm run test:parser]
  - description: Convert Backlog/Doing/Review/Done/Paused sections to one `## Tasks` list.
  - acceptance:
      - [ ] Migration preserves IDs and detail links.
      - [ ] Migration output is sorted by task ID.
      - [ ] `## Notes` content remains unchanged.
  - steps:
      - [ ] Read legacy sections and infer status.
      - [ ] Rewrite into single list and normalize fields.
      - [ ] Add idempotency check for repeated runs.
    ```md
    This command should support batch migration with predictable output.
    It should not edit unrelated markdown sections.
    ```
