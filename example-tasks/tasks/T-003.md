# T-003

  - role: infra
  - impact: medium
  - estimatedEffort: 4d
  - prerequisites: [T-001]
  - blocking: []
  - filesAffected: [scripts/analyze-tasks.ts]
  - testsRequired: [npm run lint, npm run check-types]
  - summary: Generate a conflict report with safe parallel pairs and dependency signals.
  - description: |
      ## Acceptance
      - [ ] Output includes tasks, conflicts, parallelizablePairs, testGaps, metrics.
      - [ ] Conflict severities are low/medium/high with reasons.
      - [ ] Analyzer can run on demand without CI.

      ## Steps
      - [x] Define output schema.
      - [ ] Implement overlap detection from `domains`.
      - [ ] Add dependency graph checks from `dependsOn`.

      ## Notes
      This task optimizes AI planning cost by producing a compact report.
      It should avoid requiring full repository scans for every planning cycle.
