# T-004

  - role: frontend
  - impact: high
  - estimatedEffort: 3d
  - prerequisites: [T-001]
  - blocking: []
  - filesAffected: [src/html/webviewScript.js, src/html/webview.html, src/html/style.css]
  - testsRequired: [npm run lint]
  - summary: Render kanban columns from grouped `status` values instead of markdown sections.
  - description: |
      ## Acceptance
      - [ ] Drag-and-drop updates only task `status`.
      - [ ] Empty statuses still render predictable columns.
      - [ ] Board remains usable on desktop and mobile.

      ## Steps
      - [ ] Build status grouping in frontend state.
      - [ ] Keep card actions unchanged.
      - [ ] Add regression test scenarios for status transitions.

      ## Notes
      The UI should preserve current usability while changing data source semantics.
      Column order must be deterministic to reduce visual noise.
