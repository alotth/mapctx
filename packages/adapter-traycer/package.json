{
  "name": "@mapctx/adapter-traycer",
  "version": "0.0.0",
  "private": true,
  "description": "Pure MapCtx-to-Traycer dispatch and ticket projection adapter",
  "type": "commonjs",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/**/*.test.js",
    "dispatch-skeleton": "tsx src/dispatch-skeleton.ts"
  },
  "dependencies": {
    "@mapctx/planner": "file:../planner",
    "@mapctx/protocol": "0.1.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
