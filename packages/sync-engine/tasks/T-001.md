# T-001

- objective: map local dates to GitHub Project DATE fields during push
- scope:
  - add config support for `startDateFieldId`, `dueDateFieldId`, `completedDateFieldId`
  - add GraphQL mutation helpers for set/clear date values
  - update push flow to set and clear date fields by task values
- acceptance:
  - push writes start/due/completed dates when configured
  - push clears date values when local values are missing
