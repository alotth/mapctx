# T-003

- objective: validate sync against the real GitHub Project
- steps:
  - fill `startDateFieldId` and `dueDateFieldId` in runtime config
  - run `kanban-sync-engine push --dry-run`
  - run `kanban-sync-engine push`
  - verify roadmap bars in GitHub Project view
- done-when:
  - project items show timeline bars based on start/due
