# T-006

- objective: add regression coverage for Project date sync
- scope:
  - unit tests for date-field mutation inputs
  - integration-style tests for push flow decisions (set vs clear)
  - dry-run output checks for date operations
- acceptance:
  - test suite catches regressions in roadmap date sync behavior
