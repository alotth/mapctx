# T-007

  - objective:
      - [x] Support dynamic `allowedStatuses` with runtime validation.
      - [x] Support configurable `completionStatuses` without hardcoded `done`.
      - [x] Preserve custom statuses across `status/pull/push/bootstrap/reconcile`.
      - [x] Add regression and custom-workflow tests.
      - [x] Update docs and config examples for default + full replacement workflows.

  - implementationNotes:
      - Added status normalization and validation helpers in `src/statuses.ts`.
      - Extended config and type model to accept dynamic status keys.
      - Updated sync semantics for completed date using configured completion statuses.
      - Added `src/status-workflows.test.ts` and `npm test` script.
