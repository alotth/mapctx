# T-001

  - role: feature
  - impact: high
  - estimatedEffort: 2d
  - prerequisites: []
  - blocking: [T-002, T-003]
  - filesAffected: [.opencode/plugins/mapcs-session-sync.ts, packages/opencode-plugin/**, hooks/, docs/, packages/sync-engine/README.md]
  - testsRequired: [zsh -lic "npm run build --workspace @mapctx/sync-engine", zsh -lic "npm run build --workspace @mapctx/opencode-plugin"]
  - summary: Harden session-start sync behavior and plugin integration so auto-sync is safe, observable, and non-blocking.
  - description: |
      ## Acceptance
      - [ ] OpenCode session-start flow runs `status` before any pull/write operation.
      - [ ] Plugin handles missing CLI/config/tasks with clear logs and no startup break.
      - [ ] Hook templates for OpenCode and Claude/Cursor follow the same safe sequence.
      - [ ] Failure in auto-sync is reported and does not block session startup.
      - [ ] Docs include install, verify, rollback, and troubleshooting steps.

      ## Steps
      - [ ] define canonical command sequence and fallback behavior.
      - [ ] harden `.opencode/plugins/mapcs-session-sync.ts` diagnostics and guardrails.
      - [ ] align `packages/opencode-plugin` behavior with hook expectations.
      - [ ] add/update hook templates under `hooks/`.
      - [ ] document setup and validation flow.
