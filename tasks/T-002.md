# T-002

  - role: backend
  - impact: high
  - estimatedEffort: 3d
  - prerequisites: [T-001]
  - blocking: [T-005, T-008]
  - filesAffected: [src/sync/cli.ts, src/sync/core.ts, src/sync/markdown.ts, src/sync/github.ts]
  - testsRequired: [npm run build, node dist/cli.js status --config sync.dev.json]
  - description: Build the sync engine package with pull, push, status, and bootstrap commands.
  - acceptance:
      - [x] CLI parses command flags.
      - [x] pull/push/status work in dry-run.
      - [ ] bootstrap is validated against real project IDs.
  - steps:
      - [x] Add package and TS config.
      - [x] Implement markdown parser and serializer.
      - [x] Implement GitHub API integration through gh.
      - [ ] Validate project status write path end-to-end.
    ```md
    Core sync commands exist and run locally, but project field write validation
    still needs end-to-end check on a real project.
    ```
