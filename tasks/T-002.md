# T-002

  - role: feature
  - impact: medium
  - estimatedEffort: 2d
  - prerequisites: [T-001]
  - blocking: []
  - filesAffected: [packages/sync-engine/src/, docs/]
  - testsRequired: [npm run test:sync-engine]
  - summary: Add task log timeline sync using GitHub issue comments.
  - description: |
      ## Product Context
      Task execution logs could be useful across tools, but GitHub comment sync is not needed for the current local-first workflow.

      ## User Story
      As an operator using remote sync, I want task logs to publish as issue comments without duplicates.

      ## Expected Outcome
      Local task logs can sync to GitHub comments deterministically when GitHub sync is resumed.

      ## Status Notes
      - [2026-05-29] Paused because GitHub status is paused and local `TASKS.md` is current source of truth.

      ## Acceptance
      - [ ] Task log schema is defined.
      - [ ] GitHub comments publish idempotently.
      - [ ] Pull/push preserves local log history and remote linkage.

      ## Steps
      - [ ] define log schema.
      - [ ] implement GitHub comment adapter.
      - [ ] add duplicate-prevention tests.
