# T-002

  - role: feature
  - impact: high
  - estimatedEffort: 3d
  - prerequisites: [T-001]
  - blocking: []
  - filesAffected: [packages/sync-engine/**, tasks/*.md, docs/]
  - testsRequired: [zsh -lic "npm run build --workspace @mapctx/sync-engine", zsh -lic "npm run test --workspace @mapctx/sync-engine"]
  - summary: Implement two-way task log timeline sync using GitHub issue comments as append-only remote events.
  - description: |
      ## Acceptance
      - [ ] local task logs support stable ID, timestamp, actor, type, and message.
      - [ ] push publishes new local log entries as deterministic issue comments.
      - [ ] pull imports new remote comments into local logs without duplicates.
      - [ ] repeated push/pull is idempotent for logs and comments.
      - [ ] docs explain body as canonical task state and comments as timeline events.

      ## Steps
      - [ ] define log schema and parser/serializer format.
      - [ ] implement local append/read log utilities.
      - [ ] implement GitHub comment adapter with idempotency markers.
      - [ ] implement reconcile policy for concurrent writes.
      - [ ] add tests for round-trip, deduplication, and conflicts.
