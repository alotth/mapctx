# T-003

  - role: feature
  - impact: medium
  - estimatedEffort: 1d
  - prerequisites: [T-001]
  - blocking: [T-004, T-005]
  - filesAffected: [packages/sync-engine/src/, packages/vscode-extension/src/, docs/]
  - testsRequired: [npm run test:sync-engine, npm run compile]
  - summary: Add iteration and assignees with backward compatibility.
  - description: |
      ## Product Context
      Planning metadata such as iteration and assignees is useful locally, but should not break older boards.

      ## User Story
      As a board maintainer, I want optional iteration and assignee fields that older task files can omit safely.

      ## Expected Outcome
      Parser, serializer, UI, and docs support optional metadata without requiring GitHub Projects.

      ## Status Notes
      - [2026-05-29] Paused because local support appears partial and Project-backed sync is not active.

      ## Acceptance
      - [ ] Parser accepts optional `iteration` and `assignees`.
      - [ ] Serializer preserves backward compatibility.
      - [ ] UI shows assignees only when present.
      - [ ] Docs describe optional use.

      ## Steps
      - [ ] audit current parser/UI support.
      - [ ] fill missing backward-compatibility tests.
      - [ ] document local-only behavior.
