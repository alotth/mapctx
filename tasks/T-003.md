# T-003

  - role: feature
  - impact: high
  - estimatedEffort: 2d
  - prerequisites: [T-001]
  - blocking: [T-004, T-005]
  - filesAffected: [packages/sync-engine/src/, packages/opencode-plugin/src/, packages/vscode-extension/src/, packages/core/src/, skills/mapctx-tasks/references/]
  - testsRequired: [zsh -lic "npm run test:sync-engine", zsh -lic "npm run compile"]
  - summary: Add `iteration` and `assignees` support across parser, sync, and UI surfaces with low-risk compatibility defaults.
  - description: |
      ## Acceptance
      - [ ] TASKS parser and serializer support `iteration` and `assignees` without breaking old files.
      - [ ] sync engine maps both fields with safe fallback when remote fields are missing.
      - [ ] OpenCode plugin and VS Code UI render both fields consistently.
      - [ ] boards without new keys still parse, render, and sync correctly.
      - [ ] skills/docs define canonical field order and semantics.

      ## Steps
      - [ ] define canonical metadata placement for new fields.
      - [ ] extend sync-engine types and markdown roundtrip support.
      - [ ] add remote mapping logic and fallback guardrails.
      - [ ] update UI surfaces for list/detail display.
      - [ ] add parse/sync compatibility coverage.
