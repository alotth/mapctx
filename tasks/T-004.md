# T-004

  - role: feature
  - impact: high
  - estimatedEffort: 2d
  - prerequisites: [T-003]
  - blocking: [T-005]
  - filesAffected: [packages/sync-engine/src/github.ts, packages/sync-engine/src/sync.ts, packages/sync-engine/src/types.ts, docs/, skills/mapctx-sync-engine/references/]
  - testsRequired: [zsh -lic "npm run test:sync-engine"]
  - summary: Use GitHub Projects GraphQL fields for sub-issue progress mapping while keeping metadata fallback compatibility.
  - description: |
      ## Acceptance
      - [ ] GraphQL layer fetches parent and sub-issue progress data.
      - [ ] sync engine writes `subIssueProgress` from remote data when available.
      - [ ] metadata fallback remains active when GraphQL data is unavailable.
      - [ ] pull/push flows remain compatible with previously synced tasks.
      - [ ] docs define precedence rules between GraphQL and metadata fallback.

      ## Steps
      - [ ] extend GraphQL query and mapping model.
      - [ ] implement precedence logic in sync merge paths.
      - [ ] keep and validate compatibility fallback behavior.
      - [ ] add tests for available/unavailable remote scenarios.
      - [ ] update sync-engine references and examples.
