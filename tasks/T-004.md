# T-004

  - role: feature
  - impact: medium
  - estimatedEffort: 1d
  - prerequisites: [T-003]
  - blocking: [T-005]
  - filesAffected: [packages/sync-engine/src/github.ts, packages/sync-engine/src/sync.ts, docs/]
  - testsRequired: [npm run test:sync-engine]
  - summary: Map sub-issue progress from GitHub Projects GraphQL.
  - description: |
      ## Product Context
      Sub-issue progress is useful when GitHub is the remote planning surface, but local MapCtx can operate without it.

      ## User Story
      As a GitHub Project user, I want epic progress to reflect remote sub-issue completion.

      ## Expected Outcome
      `subIssueProgress` can be read from GitHub Projects when that integration is resumed.

      ## Status Notes
      - [2026-05-29] Paused with GitHub Project work. Current token lacks `read:project`, and local workflow is preferred.

      ## Acceptance
      - [ ] GraphQL query reads sub-issue progress.
      - [ ] Pull maps remote progress to local epic fields.
      - [ ] Missing Project access fails with actionable guidance.

      ## Steps
      - [ ] confirm GraphQL field shape.
      - [ ] implement mapping.
      - [ ] add tests with mocked Project response.
