# T-007

  - role: quality
  - impact: high
  - estimatedEffort: 3d
  - prerequisites: [T-003, T-004]
  - blocking: [T-008]
  - filesAffected: [src/kanbanWebviewPanel.ts, src/markdownParser.ts, src/html/webviewScript.js, src/test/extension.test.ts]
  - testsRequired: [npm run lint, npm test, npm run test:parser]
  - description: Remove temporary debug instrumentation and stabilize test coverage for V2 changes.
  - acceptance:
      - [ ] debug-only logging removed from source.
      - [ ] parser and webview tests pass reliably.
      - [ ] no noisy side effects during normal usage.
  - steps:
      - [ ] remove cursor/debug log instrumentation blocks.
      - [ ] add regression tests for date and status handling.
      - [ ] run full checks and fix failures.
    ```md
    Current code contains heavy debug traces that must be cleaned before release.
    ```
