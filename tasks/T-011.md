# T-011

  - role: backend
  - impact: high
  - estimatedEffort: 2d
  - prerequisites: [T-010]
  - blocking: [T-012, T-015]
  - filesAffected: [packages/kanban-sync-engine/**]
  - testsRequired: [npm run build --workspace kanban-sync-engine, npm run test --workspace kanban-sync-engine]
  - description: Import current kanban-sync-engine into monorepo as a first-class workspace package while preserving npm package identity.
  - acceptance:
      - [x] package path is packages/kanban-sync-engine.
      - [x] npm name remains kanban-sync-engine.
      - [x] sensitive/local-only files are excluded from imported tree.
      - [x] build and tests pass in workspace mode.
  - steps:
      - [x] copy source and config from temp/kanban-sync-engine.
      - [x] remove .env, node_modules, dist, and nested git metadata.
      - [x] align scripts and repository metadata to monorepo.
