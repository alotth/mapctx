# T-011

  - role: docs
  - impact: high
  - estimatedEffort: 0.5d
  - prerequisites: []
  - blocking: []
  - filesAffected: [rules/.cursorrules]
  - testsRequired: [manual-review]
  - summary: Rewrite `rules/.cursorrules` to route task edits through the single-list contract.
  - description: |
      ## Product Context
      Cursor rules should match the same task-editing policy as the other agent rule packs.

      ## User Story
      As a Cursor user, I want the repo rules to point me at `mapctx-tasks` before task edits.

      ## Expected Outcome
      `rules/.cursorrules` is lightweight and aligned with the current task model.

      ## Acceptance
      - [x] Rule file references `mapctx-tasks` for `TASKS.md` edits.
      - [x] Rule file avoids duplicating the full task contract.
      - [x] Execution discipline matches the shared rule model.

      ## Steps
      - [x] replace old long guidance.
      - [x] keep detailed behavior in skills.
