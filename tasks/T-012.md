# T-012

  - role: architecture
  - impact: high
  - estimatedEffort: 3d
  - prerequisites: [T-011]
  - blocking: [T-013, T-014]
  - filesAffected: [packages/core/**, src/markdownParser.ts, opencode-plugin/src/kanban-roadmap.js, packages/kanban-sync-engine/src/markdown.ts]
  - testsRequired: [npm run build --workspace @project/core, npm run test --workspace kanban-sync-engine]
  - description: Create a shared core package with parser/model/status primitives and reusable markdown/html render helpers for extension and plugin surfaces.
  - acceptance:
      - [x] core exports task model types and parser/serializer APIs.
      - [x] core exports markdown-to-html helper utilities.
      - [x] duplicated parser logic is reduced in extension and engine code paths.
      - [x] package is private until project naming is finalized.
  - steps:
      - [x] define core public API and boundary.
      - [x] move parsing and normalization functions into core.
      - [x] move common markdown rendering helpers into core.
