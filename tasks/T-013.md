# T-013

  - role: frontend
  - impact: high
  - estimatedEffort: 2d
  - prerequisites: [T-012]
  - blocking: [T-017]
  - filesAffected: [packages/vscode-extension/src/**, packages/vscode-extension/package.json]
  - testsRequired: [npm run check-types --workspace @project/vscode-extension, npm run package --workspace @project/vscode-extension]
  - description: Update the VS Code extension implementation to consume parser and rendering utilities from the new core workspace package.
  - acceptance:
      - [x] extension parser imports come from core.
      - [x] workspace V2 behavior remains functionally equivalent.
      - [x] extension package and vsix generation succeed.
  - steps:
      - [x] replace local parser imports with core package imports.
      - [x] adapt extension-specific types where needed.
      - [x] run extension build and smoke checks.
