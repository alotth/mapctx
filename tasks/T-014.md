# T-014

  - role: feature
  - impact: high
  - estimatedEffort: 1d
  - prerequisites: [T-012]
  - blocking: [T-016, T-027]
  - filesAffected: [skills/mapctx-tasks/references/custom-statuses.md, skills/mapctx-tasks/references/status-and-sync.md, skills/mapctx-tasks/SKILL.md]
  - testsRequired: [npm run test:sync-engine]
  - summary: Introduce `ready-for-do` as the default pre-execution status in MapCtx workflows.
  - description: |
      ## Product Context
      Teams need a clear state between rough backlog and active execution to avoid starting under-specified work.

      ## User Story
      As an operator, I want tasks to enter a clear ready state before implementation so execution quality improves.

      ## Acceptance
      - [x] Default lifecycle documents `backlog -> ready-for-do -> doing -> review -> done`.
      - [x] Completion-state behavior remains deterministic.
      - [x] Sync mapping guidance includes the new status.

      ## Decisions Taken
      - [2026-05-29] Marked done during task cleanup after confirming current repo evidence.

      ## Steps
      - [x] update status defaults in references.
      - [x] update sync mapping guidance.
      - [x] align skill wording with new lifecycle.
