# T-015

  - role: release
  - impact: high
  - estimatedEffort: 1d
  - prerequisites: [T-011]
  - blocking: [T-016, T-017]
  - filesAffected: [.github/workflows/main.yml, .github/workflows/release-sync-engine.yml, .github/workflows/release-opencode-plugin.yml]
  - testsRequired: [workflow-lint, dry-run-release-checklist]
  - description: Split release automation by tag prefixes so extension and npm packages can publish independently in monorepo.
  - acceptance:
      - [ ] extension workflow triggers on ext-v* tags only.
      - [ ] sync engine workflow triggers on sync-v* tags.
      - [ ] OpenCode plugin workflow triggers on plugin-v* tags.
      - [ ] no workflow triggers from unrelated tags.
  - steps:
      - [ ] update extension workflow tag filters.
      - [ ] add package-specific publish workflows.
      - [ ] document required secrets per workflow.
