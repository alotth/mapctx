# T-018

  - role: feature
  - impact: medium
  - estimatedEffort: 2d
  - prerequisites: [T-002]
  - blocking: []
  - filesAffected: [packages/kanban-sync-engine/src/cli.ts, packages/kanban-sync-engine/src/sync.ts, packages/kanban-sync-engine/src/types.ts, packages/kanban-sync-engine/src/config.ts, packages/kanban-sync-engine/README.md, packages/kanban-sync-engine/DOCUMENTATION.md]
  - testsRequired: [npm run build --workspace kanban-sync-engine, npm run test --workspace kanban-sync-engine]
  - description: Add a local Obsidian export flow (A) in kanban-sync-engine and document bidirectional sync (B) as future improvement only.
  - acceptance:
      - [ ] CLI supports an Obsidian export command that writes deterministic task notes into a configured vault path.
      - [ ] Config supports Obsidian settings (vault path, folder, naming strategy) with validation and clear errors.
      - [ ] Export is one-way from TASKS.md/tasks detail files to Obsidian notes (no import in this task).
      - [ ] Docs include setup and usage for local export and explicitly label bidirectional sync as future work.
      - [ ] Future section documents option B (import/reconcile) with conflict-handling requirements, without implementing it.
  - steps:
      - [ ] define Obsidian config keys and note format contract.
      - [ ] add CLI entrypoint and export execution path in sync engine.
      - [ ] map task metadata and detail content into deterministic note output.
      - [ ] add tests for config parsing and export behavior.
      - [ ] update README and DOCUMENTATION with examples and future-work notes.
