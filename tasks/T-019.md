# T-019

  - role: feature
  - impact: high
  - estimatedEffort: 1d
  - prerequisites: [T-002]
  - blocking: []
  - filesAffected: [.opencode/, .claude/, .cursor/, packages/kanban-sync-engine/README.md, docs/]
  - testsRequired: [zsh -lic "npx --yes kanban-sync-engine status", zsh -lic "npx --yes kanban-sync-engine pull --dry-run"]
  - description: Add reusable session-start hooks for OpenCode and Claude Code/Cursor so the configured sync workflow runs automatically when a new session starts, without executing sync during task setup.
  - acceptance:
      - [ ] Hook templates exist for OpenCode and Claude Code/Cursor with the same sync behavior and current project config.
      - [ ] Hooks call the sync engine in safe mode first (status or dry-run) before any write operation.
      - [ ] Installation docs explain where to place each hook and how to enable/disable per project.
      - [ ] Failure handling is documented so a hook error does not block session startup.
      - [ ] Task does not run sync as part of setup; it only prepares and documents the hook flow.
  - steps:
      - [ ] map current sync command sequence and required config paths.
      - [ ] create OpenCode hook script/template for session start.
      - [ ] create Claude Code/Cursor hook script/template for session start.
      - [ ] add docs with install, verify, rollback, and troubleshooting.
      - [ ] validate commands manually in dry-run mode after setup is complete.
