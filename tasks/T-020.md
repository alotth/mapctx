# T-020

  - role: feature
  - impact: high
  - estimatedEffort: 2d
  - prerequisites: [T-002, T-012]
  - blocking: [T-021, T-022]
  - filesAffected: [packages/kanban-sync-engine/src/, src/markdownParser.ts, src/unifiedWebviewPanel.ts, src/html/workspaceV2.js, packages/opencode-plugin/src/kanban-roadmap.js, packages/core/browser/core-helpers.js, skills/kanban-tasks/]
  - testsRequired: [zsh -lic "npm run test:sync-engine", zsh -lic "npm run compile"]
  - description: Implement phase 2 support for iteration and assignees in parser, sync engine, extension, and OpenCode plugin with backward-compatible defaults.
  - acceptance:
      - [ ] TASKS parser and serializer support `iteration` and `assignees` without breaking existing files.
      - [ ] Sync engine maps `iteration` and `assignees` with safe fallback when remote fields are unavailable.
      - [ ] Workspace V2 and plugin display the new fields in list/detail views.
      - [ ] Existing boards without new keys continue to load and sync correctly.
      - [ ] Skill references are updated with field order and mapping guidance.
  - steps:
      - [ ] define canonical field placement for `iteration` and `assignees` in V2 contract.
      - [ ] extend sync-engine types and markdown roundtrip handling.
      - [ ] add remote mapping logic and guardrails for missing project fields.
      - [ ] update extension and plugin presentation layers.
      - [ ] add/adjust tests for parse and sync compatibility.
