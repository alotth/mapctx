# T-021

  - role: feature
  - impact: high
  - estimatedEffort: 2d
  - prerequisites: [T-020]
  - blocking: [T-022]
  - filesAffected: [packages/kanban-sync-engine/src/github.ts, packages/kanban-sync-engine/src/sync.ts, packages/kanban-sync-engine/src/types.ts, docs/, skills/kanban-sync-engine/]
  - testsRequired: [zsh -lic "npm run test:sync-engine"]
  - description: Replace metadata/manual subIssueProgress handling with real GitHub Projects GraphQL field mapping while preserving fallback behavior for older boards.
  - acceptance:
      - [ ] GraphQL queries fetch parent/sub-issue progress data for linked items.
      - [ ] Sync engine writes `subIssueProgress` from remote data when available.
      - [ ] Metadata fallback remains supported when GraphQL progress is unavailable.
      - [ ] Pull/push flows keep compatibility with previously synced tasks.
      - [ ] Docs explain precedence and fallback for progress source.
  - steps:
      - [ ] extend GitHub GraphQL layer for parent and sub-issue progress fields.
      - [ ] map fetched values into sync merge logic with clear precedence rules.
      - [ ] keep metadata parse/write path as compatibility fallback.
      - [ ] add tests for remote available/unavailable scenarios.
      - [ ] document configuration and behavior in sync references.
