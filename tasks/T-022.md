# T-022

  - role: chore
  - impact: medium
  - estimatedEffort: 1d
  - prerequisites: [T-020, T-021]
  - blocking: []
  - filesAffected: [packages/kanban-sync-engine/src/status-workflows.test.ts, packages/kanban-sync-engine/src/*.test.ts, example-tasks/, skills/kanban-tasks/references/, README.md]
  - testsRequired: [zsh -lic "npm run test:sync-engine", zsh -lic "npm run compile"]
  - description: Add migration notes and regression coverage for iteration/assignees and GraphQL subIssueProgress to ensure safe rollout and backward compatibility.
  - acceptance:
      - [ ] Test suite covers old boards and new boards with Epic stack plus phase 2 fields.
      - [ ] Migration docs include defaults for missing keys and rollout order.
      - [ ] Example tasks illustrate the new field set and compatibility behavior.
      - [ ] Release notes mention fallback semantics and non-breaking behavior.
  - steps:
      - [ ] create regression cases for legacy and mixed-format task files.
      - [ ] document migration defaults for null and empty list handling.
      - [ ] update example board snippets to include new fields.
      - [ ] capture rollout checklist in docs and references.
