# T-029

  - role: feature
  - impact: high
  - estimatedEffort: 2d
  - prerequisites: [T-027]
  - blocking: [T-030, T-037, T-040]
  - filesAffected: [packages/sync-engine/src/, packages/core/src/, packages/vscode-extension/src/]
  - testsRequired: [npm run test:sync-engine, npm test]
  - summary: Implement dependency DAG and wave planning to compute safe task execution order.
  - description: |
      ## Product Context
      Teams need an objective execution order to avoid blocking chains and wasted parallel effort.

      ## User Story
      As an operator, I want generated execution waves so I can run independent tasks in the right order.

      ## Acceptance
      - [x] Planner resolves runnable tasks by dependency graph.
      - [x] Cycles are reported with clear diagnostics.
      - [x] Output identifies next waves and blocked tasks.

      ## Decisions Taken
      - [2026-05-29] Marked done during task cleanup after confirming current repo evidence.

      ## Steps
      - [x] build task graph from `dependsOn`.
      - [x] implement topological wave generation.
      - [x] expose output for CLI and downstream consumers.
