# T-041

  - role: backend
  - impact: high
  - estimatedEffort: 1d
  - prerequisites: []
  - blocking: [T-042, T-043]
  - filesAffected: [packages/sync-engine/src, packages/sync-engine/README.md]
  - testsRequired: [npm run test:sync-engine]
  - summary: Push local `start`, `due`, and `completed` dates into GitHub Project DATE fields.
  - description: |
      ## Product Context
      The sync engine needs roadmap dates to travel from local planning files into GitHub Projects so timeline views stay useful.

      ## User Story
      As an operator, I want local task dates to populate Project fields so GitHub timelines reflect the current plan without manual re-entry.

      ## Expected Outcome
      Push writes or clears configured date fields based on local task values.

      ## Acceptance
      - [x] Push writes `start`, `due`, and `completed` dates when configured.
      - [x] Push clears remote date values when local values are missing.

      ## Decisions Taken
      - [2026-02-24] Date-field sync is config-driven through dedicated field IDs.

      ## Implementation Notes
      - Added config support for `startDateFieldId`, `dueDateFieldId`, and `completedDateFieldId`.
      - Added GraphQL mutation helpers for set and clear date operations in the sync flow.

      ## Steps
      - [x] Add config support for Project date field IDs.
      - [x] Add mutation helpers for set and clear behavior.
      - [x] Update push flow to emit date values from local tasks.
