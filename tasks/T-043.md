# T-043

  - role: ops
  - impact: high
  - estimatedEffort: 0.5d
  - prerequisites: [T-041, T-042]
  - blocking: [T-044]
  - filesAffected: [mapcs.config.json, packages/sync-engine]
  - testsRequired: [npm run test:sync-engine]
  - summary: Validate date-field sync against a real GitHub Project configuration.
  - description: |
      ## Product Context
      Configuration and docs still need one real-world validation pass before the date-field workflow can be trusted.

      ## User Story
      As an operator, I want one verified end-to-end Project setup so I know the roadmap timeline works outside unit tests.

      ## Expected Outcome
      A real Project is configured with field IDs, dry-run and live push succeed, and the timeline view shows the expected bars.

      ## Acceptance
      - [ ] Runtime config includes `startDateFieldId` and `dueDateFieldId`.
      - [ ] `mapcs push --dry-run` validates the outgoing date changes.
      - [ ] A live push results in visible timeline bars in GitHub Project.

      ## Open Decisions for Execution
      - [open] Which repository Project should remain the canonical validation target for this flow?

      ## Implementation Notes
      - Validation depends on real `mapcs` runtime config and a Project view with date fields enabled.

      ## Steps
      - [ ] Fill `startDateFieldId` and `dueDateFieldId` in runtime config.
      - [ ] Run `mapcs push --dry-run`.
      - [ ] Run `mapcs push` and verify the Project timeline view.
