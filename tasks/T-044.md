# T-044

  - role: backend
  - impact: high
  - estimatedEffort: 1d
  - prerequisites: [T-043]
  - blocking: [T-045, T-046, T-047]
  - filesAffected: [packages/sync-engine/src, packages/core]
  - testsRequired: [npm run test:sync-engine]
  - summary: Pull configured Project date fields back into the local task model.
  - description: |
      ## Product Context
      Date sync is incomplete if operators can only push dates to GitHub but cannot recover them back into local planning files.

      ## User Story
      As an operator, I want pull to read Project date fields so local `TASKS.md` stays aligned with the Project timeline.

      ## Expected Outcome
      Pull maps configured remote date values into local `start`, `due`, and completion semantics without breaking existing conflict rules.

      ## Acceptance
      - [x] Pull updates local `start` and `due` from Project field values.
      - [x] Completed date remains consistent with completion-state transitions.

      ## Decisions Taken
      - [2026-02-24] Pull preserves existing conflict and source-of-truth rules while adding Project date reads.

      ## Implementation Notes
      - Added Project date reads in the pull flow with optional field IDs.

      ## Steps
      - [x] Read configured date fields from Project item values.
      - [x] Map remote `start`, `due`, and `completed` into local task fields.
      - [x] Preserve existing conflict behavior.
