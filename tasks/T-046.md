# T-046

  - role: tests
  - impact: medium
  - estimatedEffort: 0.5d
  - prerequisites: [T-044]
  - blocking: []
  - filesAffected: [packages/sync-engine/src, packages/sync-engine/test]
  - testsRequired: [npm run test:sync-engine]
  - summary: Add regression coverage for Project date sync behavior.
  - description: |
      ## Product Context
      Date-field sync touches GraphQL writes, pull logic, and dry-run output, so regressions are easy to miss without targeted tests.

      ## User Story
      As a maintainer, I want regression coverage for date sync so roadmap behavior stays stable as the sync engine evolves.

      ## Expected Outcome
      Tests catch regressions in date-field mutation inputs, set-vs-clear behavior, and dry-run output for date operations.

      ## Acceptance
      - [ ] Test suite catches regressions in roadmap date sync behavior.

      ## Implementation Notes
      - Coverage should include unit tests for date mutation inputs and integration-style checks for push decisions.

      ## Steps
      - [ ] Add unit tests for date-field mutation inputs.
      - [ ] Add integration-style tests for set-vs-clear push decisions.
      - [ ] Add dry-run output checks for date operations.
