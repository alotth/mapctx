# T-056

  - role: product-engineering
  - impact: high
  - estimatedEffort: 1w
  - prerequisites: [T-050, T-051, T-053, T-054, T-055, T-066]
  - blocking: [T-065]
  - filesAffected: [packages, skills/mapctx-traycer, docs]
  - testsRequired: [end-to-end vertical slice, context budget assertion]
  - summary: Prove waves, forecast, and Gantt planned-vs-actual on top of walking-skeleton evidence from T-058.
  - description: |
      ## Product Context
      Architecture has value only if one real feature crosses every boundary with less operator burden.
      `T-058` already proved store and dispatch survive a manual round trip through Traycer
      across two worktrees; T-066 proves the first real cutover after path resolution and RunEvent telemetry.
      This task then proves the complete resource-aware waves, forecast, and UI flow.

      ## Expected Outcome
      Three rich tasks execute in two waves through Traycer and produce planned-versus-actual evidence.

      ## Workflow Gates (host guardrail: Product Review -> Architecture -> Program Design -> Vertical Slices)
      - product-review: approved -- this task's spec defines what is being proven and why (below).
      - architecture: approved -- ADR 0003 (planning intelligence, store authority, dispatch protocol).
      - program-design: approved -- tech plans for store/dispatch/planner and the vNext architecture artifact.
      - vertical-slices: approved -- wave1 T-067 (DOCS) + T-068 (SKILLS) in parallel worktrees; T-056 is the meta-slice.

      ## Acceptance
      - [x] Two non-conflicting tasks run in parallel worktrees via resource-aware waves.
      - [x] All worktrees share atomic MapCtx state.
      - [x] Context per common task stays below 2k tokens.
      - [x] Receipts include actual files/time and available usage/cost.
      - [x] Gantt shows plan, forecast, actual, and variance.
      - [x] One accepted decision is promoted to repository ADR.
      - [x] Four host workflow gates or explicit skip reasons are visible.

      ## Evidence (2026-09-05)
      - Wave1 ran T-067 (DOCS) and T-068 (SKILLS) in parallel worktrees (dogfood-t-067-docs @ 10abe8e,
        dogfood-t-068-skills @ 01c91db), both merged after review. Context measured: T-067 366 tokens,
        T-068 365 tokens (budget 2000, zero truncation). Receipts carried real changedFiles + wall time
        (T-067 312830ms, T-068 116387ms); usageEvents empty -- the harness exposes no per-dispatch token
        meter, so cost stays absent rather than fabricated. Gantt: both actuals under P90,
        allForecastsArePriorFallback false for the first time. Claims/dispatches/receipts all cross-worktree
        against the shared store (~/.mapctx/projects/9176b907).
      - Gap found by the receipt flow and fixed in the skill (dogfood-t-068-skills @ b71caf3): receipts with
        outcome completed apply planning in-progress -> review, so the flow must `task move --status doing`
        before work starts. Neither the brief nor the skill documented it; the FSM named it on first attempt.
      - Gap found by cross-worktree validate (T-067 report): the generated board snapshot is per-checkout
        until committed. Checkpoint cadence is now explicit: commit the canonical snapshot when the store
        moves (5d23942), or downstream worktrees drift against the shared store. T-069 registered for the
        remaining stale docs (docs/index.html, docs/thread-substrate.md).
      - ADR 0004 promoted: cutover refuses lossy imports; detail-file intent wins reconciliation.

      ## Steps
      - [x] Select MapCtx feature as dogfood epic.
      - [x] Execute, measure, review, and record gaps before adding adapters.
