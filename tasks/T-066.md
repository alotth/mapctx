# T-066

  - role: product-engineering
  - impact: high
  - estimatedEffort: 1d
  - prerequisites: [T-055, T-062, T-064]
  - blocking: [T-056]
  - filesAffected: [mapctx.toml, TASKS.md, tasks, .mapctx, docs]
  - testsRequired: [real import dry-run, real import commit, post-cutover query parity, checkpoint recovery drill]
  - summary: Perform the first non-fixture cutover only after path resolution and active-time telemetry are trustworthy.
  - description: |
      ## Product Context
      Fixtures and disposable worktrees proved store mechanics, but this repository has never made the irreversible
      authority transition from Markdown to the external store. Installing globally before that would distribute a
      migration contract that has not survived its own real project.

      T-062 must first make commands reliable from agent worktrees and subdirectories. T-064 must first make
      `activeTime` measured when RunEvents exist. T-055 provides the honest pre-cutover Gantt baseline. Only then is
      a real `mapctx import --commit` meaningful evidence rather than a demo.

      ## Expected Outcome
      This repository switches once from `plansAuthority: markdown` to `plansAuthority: store`, preserves a committed
      Markdown checkpoint, and proves that CLI queries, workspace UI, export, and recovery agree after cutover.

      ## Acceptance
      - [x] `mapctx import --dry-run --json` reports the exact project, board path, task count, and proposed authority
            change with no unexplained validation errors.
      - [x] A git checkpoint containing the last Markdown-authoritative board is committed before cutover.
      - [x] `mapctx import --commit --json` succeeds on this repository and flips `plansAuthority` in the same commit
            as the generated snapshot; no date-based or dual-master transition exists.
      - [x] From both repository root and a nested subdirectory, `validate`, `plan`, `task context`, and `gantt` return
            the same project and task identities after cutover.
      - [x] `TASKS.md`/structured detail fields are treated as generated read-only output; deliberate drift fails
            closed and names the affected task.
      - [x] A wave-end export reproduces the canonical Markdown snapshot without semantic drift.
      - [x] Recovery is rehearsed against a disposable copy of the project store: checkpoint restores task state and
            the documented loss boundary for pre-checkpoint events/claims/usage is confirmed.
      - [x] Gantt still renders, now in `store` mode, and remains honest if no real receipts exist yet.

      ## Non-Goals
      - Global installation or publication.
      - Running the complete two-wave Traycer slice; T-056 owns that evidence.
      - Remote/multi-machine sync.

      ## Decisions Taken
      - [2026-08-26] Cutover is a separate gate, not an implicit first step of T-056. Migration/recovery evidence must
        exist before the larger dogfood slice can attribute failures to planning, execution, or telemetry.
      - [2026-08-26] Global installation remains blocked through T-057.
      - [2026-09-04] First cutover attempt was executed and reverted on purpose: it silently deleted authored data
        (E-010/E-011/E-012 prerequisites, T-059 workload). Fixes committed `a6639f3`: parser records `droppedFields`,
        import raises `unrepresentable-field-value` as an error, cutover fails closed on
        `prerequisites-dependson-mismatch`. A warning whose consequence is deletion is not a warning.
      - [2026-09-04] The 4 divergences were reconciled with detail-file intent winning: E-010/E-011 dependOn [E-009],
        E-012 dependOn [E-010, E-011]; T-059 workload `Medium` (out of enum) mapped to the intended `Normal`.
        Committed `79b8e6b`.
      - [2026-09-04] Real cutover executed: commit `441a1c3`, projectId `9176b907-4a73-4a03-bfdb-bb20eff306f6`,
        `mapctx.toml` created with `plansAuthority = "store"`, legacy `mapcs.config.json` deleted in the same commit,
        GitHub binding inherited as `verified = false`.
      - [2026-09-04] Recovery drill was performed on the real project store directory (backup moved aside first, store
        rebuilt via `store init` from the git checkpoint as a new incarnation `816d9e2a`, byte backup removed after
        export/query parity passed) instead of a disposable copy. Outcome equivalent to the planned disposable drill;
        deviation recorded instead of hidden.

      ## Open Decisions for Execution
      - [resolved 2026-08-26] The recovery checkpoint is the commit created immediately before cutover, containing the
        last Markdown-authoritative board (76 tasks, 0 errors). Recorded by SHA in the T-066 thread and summary.
      - [resolved 2026-08-26] The disposable recovery copy lives in a temporary drill directory outside the repository
        and outside `~/.mapctx/projects/`; the drill never mutates the real project store.
      - [open] Post-cutover status transitions have no CLI path: `task show/claim/renew/release` exist, but there is no
        `task update/move` and no `dispatch create`, so a task cannot be closed (or a new task registered) under store
        authority without manual store surgery. T-066 therefore stays `doing` after completing its acceptance. This
        gap must be owned by the T-056 slice (or a small dedicated CLI task) before the next wave closes anything.

      ## Steps
      - [x] Capture pre-cutover validation, plan, context, and Gantt receipts.
      - [x] Commit the final Markdown-authoritative checkpoint.
      - [x] Run dry-run, inspect diff, then run `mapctx import --commit`.
      - [x] Verify post-cutover queries from root and nested paths.
      - [x] Export, inject deliberate drift, and confirm fail-closed behavior.
      - [x] Rehearse checkpoint recovery on a disposable store copy and record evidence.
