# T-074

  - role: implementation
  - impact: medium
  - estimatedEffort: 1d
  - prerequisites: []
  - blocking: []
  - filesAffected: []
  - testsRequired: []
  - summary: @mapctx/sync-engine@0.0.7 is live with @mapctx/core bundled (npm inlines file: deps); publish the vNext as a new release after T-072 P1 fixes merge.
  - description: |
      ## Product Context
      The published 0.0.7 predates the entire vNext (store, protocol, planner, forecast, budget ledger). Publishing bundles the workspace file: deps automatically (verified against 0.0.7's tarball: bundledDependencies includes @mapctx/core).

      ## Expected Outcome
      - Version bump (0.1.0 candidate) for all workspace packages.
      - npm publish of @mapctx/sync-engine with bundled @mapctx/* deps (verify install-from-registry in a temp dir before promoting to latest).
      - The mapcs stderr deprecation notice ships in this release (one cycle, then removal).
      - The mapctx-traycer skill installed globally can then reference the npm binary instead of an absolute path.

      ## Open Decisions for Execution
      - Version number and whether to publish the six packages individually or as the single bundled sync-engine tarball (current proven pattern: bundled).
      - npm account/token used for publish (the gh keyring has two accounts; the npm token is separate).
